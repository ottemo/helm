{{- if and .Values.dashboard.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "store.dashboard.fullname" . }}
  labels:
{{ include "store.dashboard.labels" . | indent 4 }}
spec:
  type: {{ .Values.dashboard.service.type }}
  ports:
    - port: {{ .Values.dashboard.service.port }}
      targetPort: {{ .Values.dashboard.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "store.dashboard.fullname" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}

---
apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: "{{ include "store.dashboard.fullname" . }}-gateway"
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      number: 80
      name: http-board
      protocol: HTTP
    tls:
      httpsRedirect: true
    hosts:
    - "{{ .Values.dashboard.DASHBOARDURL }}"
  - port:
      number: 443
      name: https
      protocol: HTTPS
    tls:
      mode: SIMPLE
      serverCertificate: /etc/istio/ingressgateway-certs/tls.crt
      privateKey: /etc/istio/ingressgateway-certs/tls.key
    hosts:
    - "{{ .Values.dashboard.DASHBOARDURL }}"
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: "{{ include "store.dashboard.fullname" . }}-vs"
spec:
  hosts:
  - "{{ .Values.dashboard.DASHBOARDURL }}"
  gateways:
  - "{{ include "store.dashboard.fullname" . }}-gateway"
  http:
  - match:
    - port: 80
    - port: 443
    route:
    - destination:
        host: {{ include "store.dashboard.fullname" . }}
        port:
          number: {{ .Values.dashboard.service.port }}
{{- end -}}